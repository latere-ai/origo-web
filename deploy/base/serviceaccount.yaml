# The interface reads no Kubernetes object, so its account is granted nothing
# and its token is not mounted.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: origoweb
automountServiceAccountToken: false
