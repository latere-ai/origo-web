## What this changes

<!-- What is different after this, for whoever uses or runs origo-web. -->

## Why

<!-- The problem it solves. Link the issue or the spec if there is one. -->

## Checklist

- [ ] `make` passes locally.
- [ ] A bug fix carries a test that fails without it.
- [ ] A new or changed screen works with JavaScript turned off.
- [ ] A new or changed environment variable is a row in `docs/configuration.md`.
- [ ] A new call to another service is described in `docs/integrations.md`.
- [ ] A change a user or an operator would notice has an entry under `Unreleased` in `CHANGELOG.md`.
